<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Allow cleartext HTTP to the Android emulator's loopback address (10.0.2.2)
        so the "Ask the Expert" feature can reach the local Next.js dev server.
        This domain block is ONLY active in the debug build because
        the manifest only references this config in the debug variant.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
